import { RunningTimeDirective } from './running-time.directive';

describe('RunningTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new RunningTimeDirective();
    expect(directive).toBeTruthy();
  });
});
